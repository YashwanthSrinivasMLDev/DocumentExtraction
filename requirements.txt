# First, install all core dependencies that other packages rely on.
# These must be at the top.
torch
torchvision
torchaudio

# Next, install `deepdoctection` and its dependencies.
deepdoctection[pt]

# Now, install the modified `detectron2` fork.
# It's best to use the `deepdoctection` fork's main branch,
# as it contains specific patches for compatibility.
git+https://github.com/facebookresearch/detectron2.git


# The rest of your dependencies can follow.
unstructured[pdf]
streamlit
pdf2image
Pillow
opencv-python
lxml
numpy
pandas
scipy